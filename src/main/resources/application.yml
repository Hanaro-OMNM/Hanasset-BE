spring:
  application:
    name: hanasset

  server:
    port: 8080

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/assignmentdb?useUniCode=yes&characterEncoding=UTF-8
    username: scott
    password: tiger
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.springframework.security: DEBUG

springdoc:
  # HTML 형식의 swagger 문서의 사용자 경로 지정
  swagger-ui:
    path: /swagger-ui.html
    groups-order: DESC # path, query, body, response 순으로 출력

  # JSON 형식의 OpenAPI 문서의 사용자 경로 지정
  api-docs:
    path: /api-docs